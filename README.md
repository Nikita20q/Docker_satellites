## Запуск приложения для автотестов

Клонируем репозиторий: git clone git@github.com:Nikita20q/Docker_satellites.git
Переходим в папку проекта: cd Docker_satellites
Переходим в ветку gRPC: git checkout gRPC
Запускаем Docker: docker compose up -d --build